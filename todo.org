* Use database/script.sql as symlink when no customization is needed
* Integrate with passbolt
* Implement checkout of worktree (in deploy) with submodules included
* Script to add forge to an project
** Doc git clone with submodules already checked out (also in worktree and edge)
** Create templates for files: mount_etna.sh, database/initialize.sql, etc
** DONE Create all symlinks doc to be used in project that will use forge - create a fuction ("genesis" for repository)
** DONE Doc checkout .mise-en-place.conf
** DONE Rename forge.sh to mount_etna.sh
* DONE Review symlinks
** DONE Use <ROOT_PROJECT>/doc symlink to wiki of project
** DONE Review the creation of secure in .credentials/upstream/encrypted/secure (or using .credentials/upstream/secure) -> .credentials/secure
** DONE Review use of .credentials/secret_output to not colide with .credentials/secure (sec in start of words) - also think about use secret_output in /etc/<FORGE_SYSTEM>
** DONE Review .credentials/encrypted and .credentials/pubkey - maybe use .credentials/upstream/<PATH> instead
* Create script to database
** Create database
** create groups
*** dba
*** sysforge
*** gis_group
** create users
*** <dev team>
** create schemas
*** public with qgis tables
*** public with postgis
